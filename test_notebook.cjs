const fs = require('fs');
const json = fs.readFileSync('react-example.json', 'utf8'); // Wait, the attachment might be in a different file?
// Actually I don't know where the uploaded file is stored.
